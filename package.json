{
  "name": "smara-docs-site",
  "private": true,
  "version": "1.20.65",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev . --host 0.0.0.0",
    "docs:dev": "vitepress dev . --host 0.0.0.0",
    "build": "vitepress build .",
    "docs:build": "vitepress build .",
    "preview": "vitepress preview . --host 0.0.0.0",
    "docs:preview": "vitepress preview . --host 0.0.0.0",
    "docs:audit": "node ../scripts/audit-docs-cli.mjs",
    "docs:check": "npm run docs:build && npm run docs:audit"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
